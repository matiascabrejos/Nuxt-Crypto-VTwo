import vuex from "vuex";
import axios from "axios";

const createStore = () => {
  return new vuex.Store({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    getters: {
    },
  });
};

export default createStore;